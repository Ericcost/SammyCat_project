<%= form_with(model: join_table_order_item) do |form| %>
  <% if join_table_order_item.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(join_table_order_item.errors.count, "error") %> prohibited this join_table_order_item from being saved:</h2>

      <ul>
        <% join_table_order_item.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
