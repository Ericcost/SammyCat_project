<!DOCTYPE html>
<html>
  <head>
    <title>SammyCatProject</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <script defer src="https://unpkg.com/alpinejs@3.x.x/dist/cdn.min.js%22%3E%22%3E"></script>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>

      <nav class="navbar navbar-expand-lg bg-warning">
      <div class="container-fluid">
        <%= link_to "Sammy'Shop", root_path, class: 'navbar-brand font-monospace' %></a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
          <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <li class="nav-item">
              <%= link_to "Accueil", root_path, class: 'nav-link'  %>
            </li>
            <li class="nav-item">
            </li>
            <% if current_user %>
              <li class="nav-item">
                <%= button_to "Mon Profil", user_path(current_user.id), method: :get, class: 'btn btn-link nav-link' %></li>
              </li>
              <li class="nav-item">
                <%= button_to "Se déconnecter", destroy_user_session_path, method: :delete, class: 'btn btn-link nav-link' %></li>
              </li>
            <% else %> 
              <li class="nav-item">
                <%= button_to "S'inscrire", new_user_registration_path, method: :get, class: 'btn btn-link nav-link' %></li>
              </li>
              <li class="nav-item">
                <%= button_to "Se connecter", new_user_session_path, method: :get, class: 'btn btn-link nav-link' %></li>
              </li>            
            <% end %>
          </ul>
        </div>
      </div>
    </nav>

    <% if current_user %>
      <%= button_to "Panier", cart_path(current_user_cart.id), method: :get, class: 'btn btn-link nav-link' %></li>
    <% end %>


    <%= render partial: "carts/cart" if @render_cart %>
    <%= yield %>
  </body>
</html>
